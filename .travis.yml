language: android

android:
  components:
    - tools # needs to be first to get new package versions
    - build-tools-25.0.2
jdk:
  - oraclejdk8
script:
  - cd outlay && ./gradlew build connectedCheck