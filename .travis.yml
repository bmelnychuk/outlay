language: android

android:
  components:
    - tools # needs to be first to get new package versions
    - platform-tools
    - tools
    - build-tools-25.0.2
    - android-25
  licenses:
    - '.+'
jdk:
  - oraclejdk8
os:
  - linux
script:
  - cd outlay && ./gradlew build connectedCheck